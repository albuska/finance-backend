const {getGeneralBalance} = require("./getGeneralBalance");
const {postBalance} = require("./postBalance"); 
const {detailsBalance} = require("./detailsBalance"); 

module.exports = {
    getGeneralBalance,
    postBalance,
    detailsBalance,
};